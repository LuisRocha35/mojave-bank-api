spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mojave_bank
    username: courier
    password: newvegas
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true